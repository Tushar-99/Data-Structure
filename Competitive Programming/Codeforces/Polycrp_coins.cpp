#include<bits/stdc++.h>

using namespace std;

int main(){

    int t , num , diff = INT_MAX;
    cin >> t;
    while (t){
        cin >> num;

        if(num % 3 == 0)
            cout << floor(num / 3) <<   
    }
    
}